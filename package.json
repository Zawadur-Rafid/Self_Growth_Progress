{
  "name": "self-growth-tracker",
  "version": "1.0.0",
  "description": "Open `index.html` in a browser to run the app. Data is persisted in `localStorage` (key `sg_skills_v1`).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.22.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
